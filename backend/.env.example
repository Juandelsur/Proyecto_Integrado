# ==============================================================================
# PLANTILLA DE VARIABLES DE ENTORNO - SCA HOSPITAL
# ==============================================================================
# Copia este archivo como .env y completa los valores según tu entorno
# cp .env.example .env

# ==============================================================================
# DJANGO CORE
# ==============================================================================

# Clave secreta de Django (CAMBIAR EN PRODUCCIÓN)
# Generar nueva: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=django-insecure-CHANGE-THIS-IN-PRODUCTION

# Modo debug (True para desarrollo, False para producción)
DEBUG=True

# Hosts permitidos (separados por comas)
# Desarrollo: localhost,127.0.0.1
# Producción: *.railway.app,tu-dominio.com
ALLOWED_HOSTS=localhost,127.0.0.1

# Orígenes CSRF confiables (CRÍTICO para Django 4+ con HTTPS)
# Desarrollo: http://localhost
# Producción: https://tu-proyecto.railway.app,https://tu-dominio.com
CSRF_TRUSTED_ORIGINS=http://localhost

# ==============================================================================
# BASE DE DATOS
# ==============================================================================

# OPCIÓN A: PostgreSQL Local (Desarrollo)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=sca_hospital
DB_USER=sca_user
DB_PASSWORD=sca_password
DB_HOST=localhost
DB_PORT=5432

# OPCIÓN B: PostgreSQL en NeonTech (Producción)
# Descomenta y completa con tu URL de NeonTech
# DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# OPCIÓN C: SQLite (Desarrollo simple)
# DB_ENGINE=django.db.backends.sqlite3
# DB_NAME=db.sqlite3

# ==============================================================================
# JWT (JSON Web Tokens)
# ==============================================================================

# Tiempo de vida del token de acceso (en minutos)
JWT_ACCESS_TOKEN_LIFETIME=60

# Tiempo de vida del token de refresco (en minutos)
JWT_REFRESH_TOKEN_LIFETIME=1440

# ==============================================================================
# CORS (Cross-Origin Resource Sharing)
# ==============================================================================

# Permitir todos los orígenes (True para desarrollo, False para producción)
CORS_ALLOW_ALL=True

# Orígenes permitidos (separados por comas)
# Desarrollo: http://localhost:3000,http://localhost:5173
# Producción: https://tu-frontend.vercel.app,https://tu-dominio.com
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# ==============================================================================
# NOTAS IMPORTANTES
# ==============================================================================

# 1. NUNCA subas el archivo .env al repositorio (está en .gitignore)
# 2. En producción (Railway), configura estas variables en el dashboard
# 3. Cambia SECRET_KEY en producción por una clave única y segura
# 4. Usa DEBUG=False en producción
# 5. Configura ALLOWED_HOSTS con tus dominios reales
# 6. Usa DATABASE_URL para PostgreSQL en producción (NeonTech)
# 7. CSRF_TRUSTED_ORIGINS debe incluir https:// en producción

